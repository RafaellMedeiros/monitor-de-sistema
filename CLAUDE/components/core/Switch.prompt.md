Boolean toggle for settings rows — alert switches, preference flags.

```jsx
<Switch checked={alertCpu} onChange={setAlertCpu} />
```

36×20px track; accent when on, `bg-track` when off; white knob slides with the standard ease.
