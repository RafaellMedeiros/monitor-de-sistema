Soft-filled uppercase pill for severities in the event log and status cells.

```jsx
<Badge level="crit" />            {/* → CRÍTICO */}
<Badge level="warn">ALERTA</Badge>
```

Levels: `ok` · `warn` · `crit` · `info`. Each maps to a status color + soft tint; omit children to use the default label.
